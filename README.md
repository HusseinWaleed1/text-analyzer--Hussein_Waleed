# Text Analyzer

A simple Python script that analyzes a text file and returns basic statistics.

## How to Run

1. Make sure Python 3 is installed
2. Put the text you want to analyze inside `sample_text.txt`
3. Run the script:

```bash
python main.py
